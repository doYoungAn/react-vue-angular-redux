export const all = (state) => {
    return state;
}

export const count = (state) => {
    return state.count;
}

export const owner = (state) => {
    return state.owner;
}

export const users = (state) => {
    return state.users;
}